logging:
  level:
    org.springframework.cloud.gateway: DEBUG

server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      httpclient:
        wiretap: true
      httpserver:
        wiretap: true
      routes:
        - id: main_service_route
          uri: lb://main-service
          predicates:
            - Path=/admin/categories/**,/admin/compilations/**,/admin/events/**,/categories/**,/comments/**,/compilations/**,/events/**,/users/**
        - id: user_service_route
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**